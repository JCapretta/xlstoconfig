conf t
#interface config
{% for interface in interfaces %}
interface {{ interface.interface }}
    description "{{interface.description}}"
    encapsulation dot1q {{interface.tag}}
    ip address {{interface.ip_address}} {{interface.cidr_mask|cidr_to_netmask}}
{% endfor %}


